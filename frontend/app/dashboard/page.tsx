import Dashboard from "./Dashboard";

export const metadata = {
  title: "Pipeline — Jill's Plumbing",
  description: "Contractor dashboard for incoming intake and quote review.",
};

export default function DashboardPage() {
  return <Dashboard />;
}
